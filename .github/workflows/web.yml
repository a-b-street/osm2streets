name: Deploy web

on:
  push:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2

      - name: Install wasm-pack
        uses: jetli/wasm-pack-action@v0.3.0

      - name: Build web app
        run: |
          cd tests-web
          wasm-pack build --release --target web ../osm2streets-js

      - name: Publish
        uses: peaceiris/actions-gh-pages@v3
        with:
          github_token: ${{ secrets.GITHUB_TOKEN }}
          # This will also publish serve_locally.sh and README.md, which is
          # fine. As long as this chases symlinks correctly, it'll be fine.
          publish_dir: ./tests-web/
